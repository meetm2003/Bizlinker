import 'package:flutter/material.dart';

class IndustryMarketplaceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Industry Specific Marketplace",
        style: TextStyle(color: Colors.white, fontSize: 20),
      ),
    );
  }
}